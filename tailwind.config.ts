const config = {
  theme: {
    extend: {
      fontFamily: {
        "noto-sans": ['var(--font-noto-sans)'],
        "udp-mincho": ['var(--font-biz-udpmincho)'],
      }
    },
  },
}

export default config